import { Heart, Settings, UserRound } from 'lucide-react'
import { useEffect, useState } from 'react'
import { PropertyCard } from '../components/property/PropertyCard'
import { useAuth } from '../context/AuthContext'
import { api } from '../services/api'
import type { Property } from '../types'
export function ProfilePage(){const auth=useAuth();const [tab,setTab]=useState<'profile'|'saved'>('profile');const [saved,setSaved]=useState<Property[]>([]);const [feedback,setFeedback]=useState('');useEffect(()=>{if(tab==='saved')api.favorites().then(r=>setSaved(r.items))},[tab]);if(!auth.user)return <div className="empty page-state">Please sign in to view your profile.</div>;async function update(form:FormData){try{await api.updateProfile(Object.fromEntries(form) as Record<string,string>);setFeedback('Profile updated successfully.')}catch(error){setFeedback(error instanceof Error?error.message:'Unable to update profile.')}}return <div className="profile-page"><aside><div className="profile-avatar">{auth.user.firstName[0]}{auth.user.lastName[0]}</div><h2>{auth.user.fullName}</h2><p>{auth.user.email}</p><button className={tab==='profile'?'active':''} onClick={()=>setTab('profile')}><UserRound/> Profile</button><button className={tab==='saved'?'active':''} onClick={()=>setTab('saved')}><Heart/> Saved properties</button></aside><main>{tab==='profile'?<><p className="eyebrow green">ACCOUNT</p><h1>Profile details</h1><form action={update} className="profile-form"><div className="form-row"><label>First name<input name="firstName" defaultValue={auth.user.firstName}/></label><label>Last name<input name="lastName" defaultValue={auth.user.lastName}/></label></div><label>Email<input value={auth.user.email} disabled/></label><label>Phone number<input name="phone" defaultValue={auth.user.phone}/></label><button className="primary-button"><Settings/> Save changes</button>{feedback&&<div className="form-feedback">{feedback}</div>}</form></>:<><p className="eyebrow green">YOUR COLLECTION</p><h1>Saved properties</h1>{saved.length?<div className="property-grid saved-grid">{saved.map(item=><PropertyCard key={item.id} property={item}/>)}</div>:<div className="dashboard-empty">Properties you save will appear here.</div>}</>}</main></div>}
